---
sidebar_position: 2
sidebar_label: Endpoints
---

# Endpoints

Base URL for all endpoints: `https://center-be.stamford.dev/api`

For example:

```
https://center-be.stamford.dev/api/get_file
```

export const Highlight = ({ children, color }) => (
	<span
		style={{
			backgroundColor: color,
			borderRadius: "2px",
			color: "#fff",
			padding: "0.2rem",
		}}
	>
		{children}
	</span>
);
